<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-r from-blue-100 via-green-100 to-pink-100">

    <!-- Navbar -->
    <nav class="fixed top-0 left-0 z-10 w-full p-4 bg-blue-600">
        <div class="flex items-center justify-between max-w-6xl mx-auto text-white">
            <a href="/" class="text-2xl font-semibold">StoryGen</a>
            <div class="space-x-6">
                <% if (user) { %>
                    <a href="/profile" class="hover:text-blue-200">Profile</a>
                    <% if (user.role === 'admin') { %>
                        <a href="/admin" class="hover:text-blue-200">Admin</a>
                    <% } %>
                    <a href="/logout" class="hover:text-blue-200">Logout</a>
                <% } else { %>
                    <a href="/login" class="hover:text-blue-200">Login</a>
                    <a href="/register" class="hover:text-blue-200">Register</a>
                <% } %>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <div class="flex flex-col items-center justify-center w-full h-full mt-24">
        <div class="w-full max-w-3xl p-8 bg-white border-2 border-gray-200 shadow-2xl rounded-2xl">
            <h1 class="mb-6 text-4xl font-bold text-center text-transparent text-gradient bg-gradient-to-r from-pink-500 to-yellow-500 bg-clip-text">
                Your Generated Story
            </h1>
            <p class="mb-6 text-lg text-gray-700"><%= story %></p>
            <a href="/" class="px-6 py-3 text-white bg-blue-600 rounded-md hover:bg-blue-500">Back to Home</a>
        </div>
    </div>

</body>
</html>
